use svelte not sveltekit, or react with tanstack (query & route)

setup mode > modify isi config.yaml file and can be restart the app
runner mode > manage database, cache, etc

make PORJECT_BRIEF_MVP_v0.md, only use embbed mode and targeting the mvp features only (admin dashboard, setup & runner, onboarding wizard, local cache, local database, local mqtt, and other basic feature), not fully sass but only single reusable executable, withe development time about 3 month.